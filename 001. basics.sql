/* 
The BIG-6 clauses fo a SQL statement [SFWGHO]:

SELECT column_name
FROM table_name
WHERE logical_condition_record_level (Use AND / OR)
GROUP BY column_name
HAVING logical_condition_grou p_level
ORDER BY column_name
*/

-- ctrl+B to auto format the code.
-- SQL is case insensitive.
-- USE database_name to activate that database.
-- Comments and aliases make code readable